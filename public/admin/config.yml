backend:
  name: github
  repo: efeitodigital2-bit/afiliados-blog-caraacaradigital
  branch: main
  site_domain: caraacaradigital.com
  # Usamos a API própria e segura que criamos para o seu domínio na Vercel
  base_url: https://caraacaradigital.com
  auth_endpoint: api/auth

# Permite rodar o CMS localmente com 'npx decap-cms-proxy-server'
local_backend: true

# Configurações de Mídia
media_folder: "public/uploads"
public_folder: "/uploads"

# Coleções de Conteúdo
collections:
  - name: "blog"
    label: "Artigos"
    label_singular: "Artigo"
    folder: "src/content/blog"
    create: true
    slug: "{{slug}}"
    format: "frontmatter"
    fields:
      - { label: "Título", name: "title", widget: "string" }
      - { label: "Descrição (SEO)", name: "description", widget: "text" }
      - { label: "Data de Publicação", name: "pubDate", widget: "datetime", type: "date", format: "YYYY-MM-DD", default: "" }
      - { label: "Imagem de Destaque", name: "heroImage", widget: "image", required: false }
      - { label: "Categoria", name: "category", widget: "select", options: ["politica", "economia", "cidadania", "cotidiano", "opiniao"], default: "politica", required: false }
      - { label: "Autor", name: "author", widget: "string", default: "Redação Cara a Cara Digital" }
      - { label: "Corpo do Artigo", name: "body", widget: "markdown" }
